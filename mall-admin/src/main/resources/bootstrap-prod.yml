spring:
  cloud:
    config:
      profile: prod #Enable environment name
      label: master #Branch name
      name: admin #Configure file name
      discovery:
        enabled: true
        service-id: mall-config
eureka:
  client:
    service-url:
      defaultZone: http://mall-registry:8001/eureka/
  instance:
    prefer-ip-address: true