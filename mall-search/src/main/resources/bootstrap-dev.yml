spring:
  cloud:
    config:
      profile: dev #Enable environment name
      label: master #Branch name
      name: search #Configure file name
      discovery:
        enabled: true
        service-id: mall-config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/